const canvas=document.getElementById('canvas'),ctx=canvas.getContext('2d');canvas.width=800;canvas.height=600;let ball={x:400,y:300,dx:4,dy:-4,r:8},paddle={x:350,y:550,w:100,h:15,speed:8},bricks=[],score=0,lives=3,gameOver=false;const brickRows=5,brickCols=10,brickW=70,brickH=20,brickPadding=10,brickOffsetTop=50,brickOffsetLeft=35;function initBricks(){bricks=[];for(let r=0;r<brickRows;r++){bricks[r]=[];for(let c=0;c<brickCols;c++){bricks[r][c]={x:0,y:0,status:1}}}}function drawBall(){ctx.beginPath();ctx.arc(ball.x,ball.y,ball.r,0,Math.PI*2);ctx.fillStyle='#fff';ctx.fill();ctx.closePath()}function drawPaddle(){ctx.fillStyle='#fff';ctx.fillRect(paddle.x,paddle.y,paddle.w,paddle.h)}function drawBricks(){for(let r=0;r<brickRows;r++){for(let c=0;c<brickCols;c++){if(bricks[r][c].status===1){const x=c*(brickW+brickPadding)+brickOffsetLeft;const y=r*(brickH+brickPadding)+brickOffsetTop;bricks[r][c].x=x;bricks[r][c].y=y;ctx.fillStyle=['#e74c3c','#e67e22','#f39c12','#27ae60','#3498db'][r];ctx.fillRect(x,y,brickW,brickH)}}}}function collisionDetection(){for(let r=0;r<brickRows;r++){for(let c=0;c<brickCols;c++){const b=bricks[r][c];if(b.status===1){if(ball.x>b.x&&ball.x<b.x+brickW&&ball.y>b.y&&ball.y<b.y+brickH){ball.dy=-ball.dy;b.status=0;score+=10}}}}}function draw(){ctx.fillStyle='#1e1e1e';ctx.fillRect(0,0,canvas.width,canvas.height);drawBricks();drawBall();drawPaddle();collisionDetection();ctx.fillStyle='#fff';ctx.font='20px Arial';ctx.fillText(`Score: ${score}`,20,30);ctx.fillText(`Lives: ${lives}`,canvas.width-100,30);if(ball.x+ball.dx>canvas.width-ball.r||ball.x+ball.dx<ball.r)ball.dx=-ball.dx;if(ball.y+ball.dy<ball.r)ball.dy=-ball.dy;else if(ball.y+ball.dy>canvas.height-ball.r){if(ball.x>paddle.x&&ball.x<paddle.x+paddle.w)ball.dy=-ball.dy;else{lives--;if(lives===0){gameOver=true;alert('Game Over! Score: '+score);document.location.reload()}else{ball.x=400;ball.y=300;ball.dx=4;ball.dy=-4;paddle.x=350}}}ball.x+=ball.dx;ball.y+=ball.dy;if(!gameOver)requestAnimationFrame(draw)}document.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'&&paddle.x>0)paddle.x-=paddle.speed;if(e.key==='ArrowRight'&&paddle.x<canvas.width-paddle.w)paddle.x+=paddle.speed});document.addEventListener('mousemove',e=>{const rect=canvas.getBoundingClientRect();const mouseX=e.clientX-rect.left;if(mouseX>0&&mouseX<canvas.width)paddle.x=mouseX-paddle.w/2});initBricks();draw();console.log('🎮 Breakout Ready');
