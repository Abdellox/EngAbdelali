let board=[],selected=null,turn=1;function init(){board=Array(8).fill().map(()=>Array(8).fill(0));for(let r=0;r<3;r++)for(let c=0;c<8;c++)if((r+c)%2===1)board[r][c]=2;for(let r=5;r<8;r++)for(let c=0;c<8;c++)if((r+c)%2===1)board[r][c]=1;render()}function render(){const grid=document.getElementById('board');grid.innerHTML='';for(let r=0;r<8;r++){for(let c=0;c<8;c++){const cell=document.createElement('div');cell.className=`cell ${(r+c)%2===0?'light':'dark'}`;if(board[r][c]===1){const piece=document.createElement('div');piece.className='piece red';cell.appendChild(piece)}if(board[r][c]===2){const piece=document.createElement('div');piece.className='piece black';cell.appendChild(piece)}cell.onclick=()=>select(r,c);if(selected&&selected[0]===r&&selected[1]===c)cell.classList.add('selected');grid.appendChild(cell)}}document.getElementById('turn').textContent=`${turn===1?'Red':'Black'}'s Turn`}function select(r,c){if(selected){const[sr,sc]=selected;if(Math.abs(r-sr)===1&&Math.abs(c-sc)===1&&board[r][c]===0){board[r][c]=board[sr][sc];board[sr][sc]=0;turn=turn===1?2:1}selected=null}else if(board[r][c]===turn){selected=[r,c]}render()}init();console.log('🔴 Checkers Ready');
