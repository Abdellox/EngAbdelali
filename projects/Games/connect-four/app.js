const rows=6,cols=7;let board=[],currentPlayer=1,gameOver=false;function init(){board=Array(rows).fill().map(()=>Array(cols).fill(0));currentPlayer=1;gameOver=false;render()}function render(){const grid=document.getElementById('grid');grid.innerHTML='';for(let r=0;r<rows;r++){for(let c=0;c<cols;c++){const cell=document.createElement('div');cell.className='cell';if(board[r][c]===1)cell.classList.add('player1');if(board[r][c]===2)cell.classList.add('player2');cell.onclick=()=>drop(c);grid.appendChild(cell)}}document.getElementById('turn').textContent=`Player ${currentPlayer}'s Turn`;document.getElementById('turn').style.color=currentPlayer===1?'#e74c3c':'#f39c12'}function drop(col){if(gameOver)return;for(let r=rows-1;r>=0;r--){if(board[r][col]===0){board[r][col]=currentPlayer;render();if(checkWin(r,col)){gameOver=true;alert(`Player ${currentPlayer} wins!`);return}currentPlayer=currentPlayer===1?2:1;return}}}function checkWin(row,col){const dirs=[[0,1],[1,0],[1,1],[1,-1]];for(let[dr,dc]of dirs){let count=1;for(let i=1;i<4;i++){const r=row+dr*i,c=col+dc*i;if(r>=0&&r<rows&&c>=0&&c<cols&&board[r][c]===currentPlayer)count++;else break}for(let i=1;i<4;i++){const r=row-dr*i,c=col-dc*i;if(r>=0&&r<rows&&c>=0&&c<cols&&board[r][c]===currentPlayer)count++;else break}if(count>=4)return true}return false}init();console.log('🔴 Connect Four Ready');
